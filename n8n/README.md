# n8n Workflows

Workflow exports are stored in `workflows/`. Import order, credentials, activation order, and webhook notes will be documented as each stage lands. Exports must never contain credential values.
